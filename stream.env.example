# Place at ~/.config/cto-stream/stream.env  (chmod 600)
# SRC must match <source-password> in icecast.xml
# LIS is the unguessable mount token — it doubles as the "password" to listen
SRC=your_icecast_source_password
LIS=your_random_mount_token
